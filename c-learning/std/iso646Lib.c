/*
 * =======================================
 *  iso646.h【1995】
 * =======================================
 */

#include <iso646.h>
#include <stdio.h>

/**
 * ISO 646 是一种国际标准字符集，它是 ASCII 的一个扩展，旨在支持更多的国家和语言。在 C 语言中，
 * ISO 646 标准引入了一些替代的关键字和操作符，以便在某些键盘布局或字符集中更容易使用。
 */
int main() {
    const int input = 20;
    const int result = 0;

    if (result >= 0 and input > 10) {
        printf("Very Good");
    } else {
        printf("Good");
    }

    return 0;
}
